{
  "name": "wexa-fraud-graph",
  "version": "1.0.0",
  "private": true,
  "description": "Fraud ring detection explorer backed by CognoDB (openCypher over Bolt).",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "seed": "node scripts/seed.js",
    "seed:reset": "node scripts/seed.js --reset"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "neo4j-driver": "^5.23.0"
  }
}
